import { Button } from "@material-ui/core";
// Types
import { CartItemType } from "../App";

//Styles
import { Wrapper } from "../App.styles";

type Props = {
    item:CartItemType;
    // handleAddToCart:
}